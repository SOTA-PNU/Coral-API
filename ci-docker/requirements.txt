# 검증된 조합 (2026-08-28). torch/torchvision 은 Dockerfile 에서 CPU 휠로 별도 설치.
# 주의: pip 판 torch-mlir 은 넣지 않는다 — 소스 빌드와 네임스페이스가 충돌해
#       lenet5 의 양자화 relu lowering 이 조용히 pip 판으로 떨어진다.
#
# executorch 는 여기 없다 — Dockerfile 이 --no-deps 로 따로 설치한다.
# executorch 1.3.1 은 numpy>=2.0.0 과 torch>=2.12.0a0 을 요구하는데 검증된 조합은
# numpy 1.26.4 / torch 2.8.0 이라 한 줄로는 해석되지 않는다 (ResolutionImpossible).
# 참조 이미지 coral-ci:v1 안에서도 pip check 가 이 둘을 충돌로 보고한다 — 즉
# "설치된 상태로는 성립하지만 pip 가 한 번에 도달할 수 없는" 환경이고, 그 환경이
# 검증된 환경이다. 핀을 풀면 torch 가 2.13 으로 올라가며 CUDA 스택을 끌고 온다.
# 파이프라인은 executorch 의 XNNPACKQuantizer 만 쓰므로 이 불일치는 무해하다.
numpy==1.26.4
torchao==0.17.0
pillow==12.2.0
PyYAML==6.0.1
ml_dtypes==0.5.4
cmake==4.3.4
ninja==1.13.0

# MLIR 파이썬 바인딩 빌드 의존성 (MLIR_ENABLE_BINDINGS_PYTHON=ON).
# 없으면 LLVM cmake 설정이 MLIRDetectPythonEnv.cmake 에서 FATAL_ERROR 로 죽는다:
#   "not found (install via 'pip install nanobind' or set nanobind_DIR)"
# llvm-project/mlir/python/requirements.txt 가 nanobind>=2.9,<3.0 을 요구한다.
nanobind==2.12.0

# executorch 1.3.1 의 전이 의존 중 실제로 필요한 것 (torch/numpy/torchao 제외).
# pandas 와 pyarrow 는 cifar100 parquet 로더(models/_data.py)도 직접 쓴다 —
# 예전 목록에서 빠져 있어 cifar100 경로가 런타임에 깨졌다.
pandas>=2.2.2
pyarrow
ruamel.yaml
flatbuffers
expecttest
hypothesis
kgb
mpmath==1.3.0
packaging
parameterized
pytorch-tokenizers>=1.3.0
sympy
tabulate
typing-extensions>=4.10.0
hydra-core>=1.3.0
omegaconf>=2.3.0
